# Phase 2: Async Shipper
# TODO: Implement ship_log(db_path, log) — ships a single validated log to the DB with simulated latency
# TODO: Implement ship_batch(db_path, logs) — ships multiple logs concurrently using asyncio.gather()
